## Usage
```
# setup-interfaces -ar
# wget -qO- https://raw.githubusercontent.com/maxouverzou/alpine-conf/master/install | USERNAME=maxou DEVICE=/dev/vda sh -s -- initial-setup
# reboot

# wget -qO- https://raw.githubusercontent.com/maxouverzou/alpine-conf/master/install | USERNAME=maxou sh -s -- post-reboot-setup
```