## Usage
```
# setup-interfaces -a -r
# wget -qO- https://raw.githubusercontent.com/maxouverzou/alpine-conf/master/install | USERNAME=maxou sh -s -- initial-setup
# reboot

# wget -qO- https://raw.githubusercontent.com/maxouverzou/alpine-conf/master/install | USERNAME=maxou sh -s -- post-reboot-setup
```