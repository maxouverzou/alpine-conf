## Usage
```
# setup-interfaces -a -r
# wget -qO- https://raw.githubusercontent.com/maxouverzou/alpine-conf/master/install | USERNAME=foo sh -- initial-setup
# reboot

# wget -qO- https://raw.githubusercontent.com/maxouverzou/alpine-conf/master/install | USERNAME=foo sh -- post-reboot-setup
```